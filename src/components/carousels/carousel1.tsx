import { Stack, Typography } from "@mui/material";

const carousel1 = () => {
  return (
    <Stack direction={"row"}>
      <Stack>
        <Typography>present number/number of slides</Typography>
      </Stack>
    </Stack>
  );
};

export default carousel1;
